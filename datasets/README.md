# Datasets

Raw Roboflow exports and generated image/label trees are local working data and are ignored by Git.

The maintained detector dataset entry is `cleaned_ball_detection/data.yaml`. Its cleaning provenance and
summary are stored in `cleaned_ball_detection/cleaning_report.json` and
`cleaned_ball_detection/README.md`. The large row-level manifest remains local and is not committed.
