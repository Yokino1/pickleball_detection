# Applications

- `track_video.py`: maintained multi-ball detection/tracking entry point. `--input` accepts either one video
  or a directory; folder mode reuses the loaded detector and writes automatically named outputs.

The inherited side-view projection application is archived under
`legacy/handoff_projection/`. Nothing in this directory may import that archive.
