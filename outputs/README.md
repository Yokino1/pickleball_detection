# 本地运行结果

该目录只存放当前运行生成的视频和 JSONL，不提交 Git。每轮验证使用独立、能说明用途的目录名，
例如 `sideview_2026-07-22_auto/`；确认结果无用后可以整目录删除。

正式指标报告应放在 `artifacts/benchmarks/`，模型与模型卡放在 `artifacts/models/`。不要长期保留
`smoke`、`check`、`v2` 等临时结果。
